// #pragma once

// // This should be more of a container than a class...
// // For now.

// class Scene {
//     public:
//         Scene(std::vector<std::shared_ptr<Shape>> shapes) {};
//         color computeRayColor(const ray &r, double tmin, double tmax) {
//             HitStruct hit;
//             double localTmax = tmax;

//             bool hitShape = false;
//             for(int iter = 0; iter < shapes.size(); ++iter) {
//                 hitShape = true;
//             }
//         }
//     private:
//         std::vector<std::shared_ptr<Shape>> shapes;
        
// };