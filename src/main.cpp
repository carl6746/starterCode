#include <iostream>
#include "Framebuffer.cpp"

int main() {
    std::cout<<"Hello World"<<std::endl;
    // Framebuffer fb1;
    // vec3 c(1.0,0.0,0.0);
    // fb1.clearToColor(c);

    // fb1.exportToPNG("myfile.png");

    // Framebuffer fb2(1000,800);
    // fb2.exportToPNG("myfile2.png");

    return 0;
}

//We can use his cmake to target this